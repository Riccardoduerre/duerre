# My First Blog Post

This is a first post example with Markdown content rendered from a blog collection.

Using a custom static content source avoids client-side fetch overhead and makes the blog index efficient.

## Why Markdown?

Markdown is easy to author, simple to version, and keeps the content separate from the application logic.
